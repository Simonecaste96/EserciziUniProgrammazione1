// devo utilizzare un metodo ricorsivo per ricreare il problema della torre di hanoi, so che ci sono 3 pioli e devo spostare in ordine decrescente i dischi all'iterno del piolo 1 al piolo 2, usando come supporto temporaneo il piolo 3.

#include <stdio.h>

// prototipo di funzione

metodoRicorsivo(unsigned int discoDaSpostare, unsigned int pioloOrigine, unsigned int pioloTemporaneo, unsigned int pioloFinale);

int main(void) {
    
}

// funzione ricorsiva

int metodoRicorsivo(unsigned int discoDaSpostare, unsigned int pioloOrigine, unsigned int pioloTemporaneo, unsigned int pioloFinale) {

  

}